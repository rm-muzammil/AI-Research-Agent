# Secrets — never commit these
.env
.env.local
.env.*.local
*.pem

# Dependencies
node_modules/
frontend/node_modules/

# Next.js build output
frontend/.next/
frontend/out/

# n8n local data (if ever run outside Docker)
.n8n/

# OS / editor
.DS_Store
*.log
.vscode/
.idea/

# Docker volumes if bind-mounted locally
postgres-data/
n8n-data/